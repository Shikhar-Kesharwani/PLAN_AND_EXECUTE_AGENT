services:
  - type: web
    name: plan-and-execute-agent
    env: docker
    dockerfilePath: ./Dockerfile
    dockerContext: .
    envVars:
      - key: GROQ_API_KEY
        sync: false
      - key: LLM_MODEL
        value: llama3-70b-8192
    healthCheckPath: /api/health
